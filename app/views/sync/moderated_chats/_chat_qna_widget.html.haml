/ - if(moderated_chat.approved || current_user.has_role?(:host))
%li.clearfix.left
  %span.chat-img.pull-left
    - from_user_avatar = moderated_chat.from_user.uploaded_file.assets.url if moderated_chat.from_user.uploaded_file if moderated_chat.from_user
    %img.img-circle{src: from_user_avatar, width: 50}
  .chat-body.clearfix
    .header
      %strong.primary-font
        = moderated_chat.from_user.name if moderated_chat.from_user
      %small.text-muted.pull-right
        %span.glyphicon.glyphicon-time
        = "#{moderated_chat.created_at}"
    %p= moderated_chat.message