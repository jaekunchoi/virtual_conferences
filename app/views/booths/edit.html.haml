.row
	-box 6, "Edit booth", {:List => hall_booths_path(@booth.hall), :Visit => booth_path(@booth)} do
		= render 'form'

	-box 6, "View template", {:Templates => templates_path}, "inverse" do
		#template
			= image_tag @booth.template.uploaded_file.assets, :width => '100%' if @booth.template