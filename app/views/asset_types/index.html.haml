%h1 Listing asset_types

%table
  %tr
    %th Name
    %th
    %th
    %th

  - @asset_types.each do |asset_type|
    %tr
      %td= asset_type.name
      %td= link_to 'Show', asset_type
      %td= link_to 'Edit', edit_asset_type_path(asset_type)
      %td= link_to 'Destroy', asset_type, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Asset type', new_asset_type_path
