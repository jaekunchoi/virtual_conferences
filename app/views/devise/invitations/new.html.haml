.row
  - box 6, t("devise.invitations.new.header") do
    = simple_form_for(resource, :as => resource_name, :url => invitation_path(resource_name), :html => {:method => :post, :class => "form-horizontal"}, :wrapper => "horizontal", defaults: { :input_html => { class: "form-control"}, label_html: { class: "col-lg-4" } } ) do |f|
      = devise_error_messages!
      .form-inputs
        - resource.class.invite_key_fields.each do |field|
          = f.input field

      .form-actions
        = f.submit t("devise.invitations.new.submit_button"), class: "btn btn-primary"